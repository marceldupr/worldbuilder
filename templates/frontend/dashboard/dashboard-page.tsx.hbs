/**
 * Dashboard Page
 * Generated by Worldbuilder
 */

import { useQuery } from '@tanstack/react-query';
import {
  Box,
  Grid,
  Paper,
  Typography,
  Card,
  CardContent,
  CircularProgress,
} from '@mui/material';
import {
  {{#each elements}}
  {{getIcon name}} as {{pascalCase name}}Icon,
  {{/each}}
} from '@mui/icons-material';
import { api } from '../lib/api-client';

export function DashboardPage() {
  {{#each elements}}
  {{#if (hasManipulator name ../manipulators)}}
  const \{ data: {{camelCase name}}Data \} = useQuery(\{
    queryKey: ['{{camelCase name}}-count'],
    queryFn: () => api.{{camelCase name}}.getAll(\{ page: 1, limit: 1 \}),
  \});
  {{/if}}
  {{/each}}

  const stats = [
    {{#each elements}}
    {{#if (hasManipulator name ../manipulators)}}
    {
      title: '{{pluralize (pascalCase name)}}',
      value: \{{camelCase name}\}Data?.pagination.total || 0,
      icon: <{{pascalCase name}}Icon sx=\{{ fontSize: 40 }} />,
      color: '{{getColor @index}}',
    },
    {{/if}}
    {{/each}}
  ];

  return (
    <Box>
      <Typography variant="h4" fontWeight={700} mb={3}>
        Dashboard
      </Typography>

      <Grid container spacing={3}>
        \{stats.map((stat, index) => (
          <Grid item xs=\{12} sm=\{6} md=\{3} key=\{index}>
            <Card>
              <CardContent>
                <Box display="flex" justifyContent="space-between" alignItems="center">
                  <Box>
                    <Typography variant="body2" color="text.secondary" gutterBottom>
                      \{stat.title}
                    </Typography>
                    <Typography variant="h4" fontWeight=\{700}>
                      \{stat.value}
                    </Typography>
                  </Box>
                  <Box
                    sx=\{{
                      width: 64,
                      height: 64,
                      borderRadius: 2,
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'center',
                      bgcolor: `$\{stat.color}.50`,
                      color: `$\{stat.color}.main`,
                    }}
                  >
                    \{stat.icon}
                  </Box>
                </Box>
              </CardContent>
            </Card>
          </Grid>
        ))}
      </Grid>

      <Grid container spacing={3} mt={2}>
        <Grid item xs={12} md={6}>
          <Paper sx=\{{ p: 3 }}>
            <Typography variant="h6" fontWeight={600} gutterBottom>
              Welcome to {{project.name}}
            </Typography>
            <Typography variant="body2" color="text.secondary">
              {{#if project.description}}
              {{project.description}}
              {{else}}
              Your application dashboard. Use the navigation menu to manage your data.
              {{/if}}
            </Typography>
          </Paper>
        </Grid>

        <Grid item xs={12} md={6}>
          <Paper sx=\{{ p: 3 }}>
            <Typography variant="h6" fontWeight={600} gutterBottom>
              Quick Stats
            </Typography>
            <Box mt={2}>
              {{#each elements}}
              {{#if (hasManipulator name ../manipulators)}}
              <Box display="flex" justifyContent="space-between" mb={1}>
                <Typography variant="body2" color="text.secondary">
                  Total {{pluralize (pascalCase name)}}
                </Typography>
                <Typography variant="body2" fontWeight={600}>
                  \{\{{camelCase name}\}Data?.pagination.total || 0\}
                </Typography>
              </Box>
              {{/if}}
              {{/each}}
            </Box>
          </Paper>
        </Grid>
      </Grid>
    </Box>
  );
}

