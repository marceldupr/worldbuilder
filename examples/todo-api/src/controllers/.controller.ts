import { Router, Request, Response } from 'express';
import { Service } from '../services/.service';
import { CreateSchema, UpdateSchema } from '../entities/.entity';

const router = Router();
const service = new Service();





export default router;

